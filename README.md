# gmail-pubsub-handler
Gmail Pub/Sub handler for syncing emails with calendar events. Automatically processes Gmail push notifications via Google Pub/Sub, retrieves new emails, and updates related Google Calendar events in real time.
